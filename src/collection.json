{
  "$schema": "node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "crud-module": {
      "description": "Generate a CRUD module with domain templates (controller, service, DTO, etc.)",
      "factory": "../dist/crud/index#crudModule",
      "schema": "./crud/schema.json"
    }
  }
}
