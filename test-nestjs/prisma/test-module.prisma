/// test-module
model TestModule {
    id        String   @id @default(cuid())
    name      String
    email     String?  @unique
    age       Int?
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("test_modules")
}
